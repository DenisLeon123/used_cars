<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dleon.repository.PositionRepository">
    <! - Поиск позиции по марке и модели ->
    <select id="findPositionForMarkaAndModel" parameterType="Integer, Integer"
            resultType="java.util.List<dleon.dto.PositionDto>">
        select * from cars_position where c_marka= #{id} and c_model= #{id}
    </select>
</mapper>